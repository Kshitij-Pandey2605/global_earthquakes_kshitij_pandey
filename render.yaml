services:
  - type: web
    name: global-earthquake-analytics-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: API_VERSION
        value: v1
      - key: MONGODB_URI
        sync: false          # Set manually in Render dashboard (secret)
      - key: JWT_SECRET
        sync: false          # Set manually in Render dashboard (secret)
      - key: JWT_EXPIRES_IN
        value: 15m
      - key: JWT_COOKIE_EXPIRES_IN
        value: 7
      - key: JWT_REFRESH_SECRET
        sync: false          # Set manually in Render dashboard (secret)
      - key: JWT_REFRESH_EXPIRE
        value: 7d
      - key: CORS_ORIGIN
        value: "*"           # Replace with your frontend URL after deployment
      - key: RATE_LIMIT_WINDOW_MS
        value: "900000"
      - key: RATE_LIMIT_MAX
        value: "100"
